{
  "name": "cubetimer-api",
  "version": "1.0.0",
  "description": "API For cubetimer.io",
  "main": "src/index.ts",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "yarn run build",
    "start": "node build/index.js",
    "start:dev": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Bryan Mytko",
  "license": "ISC",
  "dependencies": {
    "@types/bcrypt": "^5.0.0",
    "@types/cors": "^2.8.12",
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "express": "^4.17.2",
    "pino": "^7.6.5",
    "tslint": "^6.1.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.16",
    "nodemon": "^2.0.15",
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  }
}
